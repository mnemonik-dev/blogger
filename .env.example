# Mnemonik Blogger — environment configuration
#
# Copy to `.env` for LOCAL DEVELOPMENT ONLY. In production, inject these via a
# secret manager / fabric runner — never commit real secrets. `.env` is gitignored.

# --- Runtime ---------------------------------------------------------------
# Safety default is dry-run. Set to false (or use `post --live`) to publish.
MNEMONIK_DRY_RUN=true
# Optional: local protocol-facts file used when the Mnemonik MCP is unavailable.
# MNEMONIK_FACTS_PATH=./facts.yaml

# --- Telegram channel ------------------------------------------------------
# Bot must be an ADMIN of the target channel.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHANNEL=@mnemonik

# --- Discord channel -------------------------------------------------------
# Easiest: a channel webhook URL. (Alternative: BOT_TOKEN + CHANNEL_ID.)
DISCORD_WEBHOOK_URL=
# DISCORD_BOT_TOKEN=
# DISCORD_CHANNEL_ID=

# --- X (Twitter) — API v2, requires a paid tier for write access -----------
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# --- Farcaster — via Neynar managed signer ---------------------------------
FARCASTER_NEYNAR_API_KEY=
FARCASTER_SIGNER_UUID=
